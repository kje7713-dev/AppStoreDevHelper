{
  "name": "appops-agent",
  "version": "0.1.0",
  "private": true,
  "description": "API-first App Store operations assistant for indie iOS developers",
  "scripts": {
    "dev": "cd apps/web && npm run dev",
    "build": "cd apps/web && npm run build",
    "lint": "cd apps/web && npm run lint",
    "test": "vitest run --reporter=verbose"
  },
  "devDependencies": {
    "vitest": "^3.1.3",
    "zod": "^3.25.1"
  }
}
